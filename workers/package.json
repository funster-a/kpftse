{
  "name": "kpftse-workers",
  "version": "1.0.0",
  "description": "Cloudflare Worker for sending orders to Telegram",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail"
  },
  "keywords": ["cloudflare", "workers", "telegram"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

